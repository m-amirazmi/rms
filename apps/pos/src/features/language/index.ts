// Feature: Language
//
// Manages the staff-selected UI language (`EN` | `MY`) and persists it
// across sessions via localStorage.
//
// Usage:
//   import { useLanguageStore, type Language } from "@/features/language"

export { useLanguageStore, type Language } from "./language-store"
